<%= form_with(model: premiere, local: true) do |form| %>
  <% if premiere.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(premiere.errors.count, "error") %> prohibited this premiere from being saved:</h2>

      <ul>
        <% premiere.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :budget %>
    <%= form.text_field :budget %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
